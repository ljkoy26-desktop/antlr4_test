# 목표

1. 파싱된 쿼리에 서브쿼리가 존재하는지,서브쿼리에 대한 메타데이터를 똑같이 가져올수 있는지
확인하고 싶어 
 ( 아래 구조체 참고 ) 

struct SqlStatementInfo {
	int index;
	SqlStatementType type;
	std::string sqlText;
	size_t startLine;
	size_t startColumn;
	bool bHasError;       // 문법 오류 여부
	int nDatabaseType;    // 파싱에 사용된 DB 타입 (DatabaseType enum 값)
};

struct TokenInfo {
	int index;
	std::string text;
	size_t tokenTypeId;    // ANTLR 원시 토큰 타입 ID (GetRoleFromLexerToken 재조회용)
	std::string tokenType;
	TokenRole role;
	std::string roleDesc;
	size_t line;
	size_t column;
	size_t startIndex;
	size_t stopIndex;
};



# 수정 대상
- SQLEngine.cpp
- SQLEngine.h
- TestMFCDlg.cpp
- TestMFCDlg.h


# 경로
- MFC 테스트 프로젝트: make_tree.bat 실행 후 MyProject_Structure.txt 확인

# 코딩 스타일
- Allman 스타일 (중괄호 다음 줄)
- 한 줄 제어문도 줄바꿈, 여러 줄은 중괄호
- 헝가리언 표기법

# 제외 디렉토리 (탐색/커밋 모두 제외)
- \antlr4_test\vcpkg
- \antlr4_test\bin
- \antlr4_test\obj
- \antlr4_test\참고자료

# 기타
- 기존 프로젝트입니다. 디렉토리 전체 재탐색 하지 마세요.
- 응답은 항상 한글로 해주세요.