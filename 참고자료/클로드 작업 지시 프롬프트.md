

아래 SQL Server 용 함수를 참고해서 DB2 용으로 하나 만들어줬으면해 

	afx_msg void OnBnClickedButtonMultiParseSQLServer();  // 복합 쿼리 파싱 버튼
	afx_msg void OnBnClickedButtonTokenizeSQLServer();    // 토큰화 버튼

 DB2 용 함수명은 이렇게 하면 되겠다.

	afx_msg void OnBnClickedButtonMultiParseDB2();  // 복합 쿼리 파싱 버튼
	afx_msg void OnBnClickedButtonTokenizeDB2();    // 토큰화 버튼



아래 지침을 지켜서 응답해줬으면 해.

This is an existing project.
Do NOT re-scan the entire directory.
Assume prior context from the provided summary.

# 응답은 한글로 항상 하세요.

# 코드 작성은 아래 요청 내용을 따라줘 

 앞으로 모든 C++ 코드는 
① Allman 스타일(중괄호 다음 줄)로 짜주고, 
② 한 줄짜리 제어문이라도 반드시 줄바꿈을 해줘. ( 여러줄인 경우는 중괄호로 묶어줘 )
③ 변수명은 헝가리언 표기법을 유지해줘.

// Good: Allman Style
if (nRow > nPrevRows)
{
    for (int i = 0; i < nCount; i++)
    {
        Allocate(1);
    }
}


# SQL Server용 파서, 렉서파일 위치
C:\Users\ljkoy\Desktop\Github_clone\antlr4_test\SqlParserLib\Source\DB2

# 소스내에 2개의 visual studio 프로젝트가 존재합니다.
# 대화상자형 MFC C++ 테스트 코드 작성하는 파일 위치 ( 현재 cd 로 이동한 디렉토리가 Root 디렉토리 입니다. )
# make_tree.bat 를 실행하여 추출되는 MyProject_Structure.txt 파일을 확인하여 주로 분석하게될 프로젝트를 확인하세요.
# TestCase 경로에는 각 DB별 테스트 케이스 스크립트가 존재합니다. ( 대화상자형 테스트 MFC에서 구문 테스트를 요청시에 이쪽 스크립트를 활용해주세요. )

\antlr4_test\make_tree.bat 

# Commit 요청 시에는 아래 경로는 건너뛰세요

\antlr4_test\vcpkg

# 아래 디렉토리는 분석을 건너뛰세요

\antlr4_test\vcpkg
\antlr4_test\bin
\antlr4_test\obj
\antlr4_test\참고자료



